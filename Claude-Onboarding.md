# Finance Ops Platform - מסמך חפיפה והיכרות (Onboarding Document)

מסמך זה נועד לספק סקירה מקיפה על פרויקט **Finance Ops Platform**, מטרתו, הארכיטקטורה שלו וכיצד הוא עובד. המסמך נכתב במיוחד כדי לעזור לבינה מלאכותית (כמו Claude Code) או למפתחים חדשים להיכנס לפרויקט במהירות ולהבין את כל חלקיו.

## 1. מטרת הפרויקט
הפרויקט הוא מערכת פנימית ("Zero-Cost Internal Finance & Operations Platform") לניהול פיננסי ותפעולי. המערכת מיועדת ככל הנראה לפרילנסר, סוכנות קטנה או עסק עצמאי שמעוניין לנהל את הלקוחות, האירועים/שירותים, ההכנסות, ההוצאות, והחיובים החוזרים שלו ללא תלות בשירותי צד-שלישי יקרים.
המערכת מאפשרת שליטה מלאה בנתונים הפיננסיים (Ledger, Balances), ניהול לקוחות (Clients), מעקב אחר אירועי שירות (Events), ויצירת חיובים אוטומטיים/חוזרים (Recurring Billings). בנוסף, קיימת תמיכה בהתראות דחיפה (Push Notifications).

## 2. טכנולוגיות מרכזיות (Tech Stack)
הפרויקט בנוי בגישת **MERN Stack** משודרגת, תוך שימוש ב-TypeScript מקצה לקצה.

**צד שרת (Backend):**
- **סביבת ריצה:** Node.js עם Express.
- **שפה:** TypeScript (שימוש ב-`tsx` להרצה בסביבת הפיתוח).
- **מסד נתונים:** MongoDB עם Mongoose כ-ORM.
- **אבטחה ואותנטיקציה:** `bcrypt` להצפנת סיסמאות, `jsonwebtoken` לניהול סשנים דרך טוקנים, `helmet` לאבטחת כותרות HTTP, ו-`express-rate-limit`.
- **העלאת קבצים:** `multer` יחד עם `cloudinary` לאחסון מדיה/תמונות.
- **וולידציה:** `zod`.
- **משימות רקע:** `node-cron` (סביר להניח עבור חיובים חוזרים או תזכורות).
- **התראות דחיפה:** `web-push`.

**צד לקוח (Frontend):**
- **תשתית:** React 18 עם Vite.
- **שפה:** TypeScript.
- **ניהול מצב (State):** Redux Toolkit (`react-redux`, `@reduxjs/toolkit`).
- **ניתוב (Routing):** React Router DOM.
- **עיצוב (Styling):** Tailwind CSS, מוגדר עם קבצי עזר כמו `class-variance-authority`, `clsx`, `tailwind-merge`.
- **רכיבי UI:** שימוש ב-Radix UI לפרימיטיבים נגישים, ו-Lucide React לאייקונים.
- **אנימציות:** Framer Motion.
- **תמיכה ב-PWA:** `vite-plugin-pwa` ו-`workbox` לתמיכה באופליין והתקנה כאפליקציה.
- **מערכת עיצוב (Design System):** הפרויקט משתמש במערכת עיצוב ייחודית המושפעת מאוד מ-Mastercard (מוגדרת בקובץ `design.md`). מאופיינת ברקע שמנת (`#F3F0EE`), פינות מעוגלות מאוד (40px עד 999px), טיפוגרפיה ספציפית, כפתורים בצורת גלולה (Pill) ושימוש זהיר בצבעי כתום ואפור.

## 3. מבנה צד השרת (Backend)
השרת ממוקם בתיקיית `backend/` ונקודת הכניסה היא `src/server.ts`. 

**מודלים עיקריים במסד הנתונים (`src/models/`):**
1. **User:** ניהול פרטי משתמש המערכת והאותנטיקציה.
2. **Client:** ניהול פרטי הלקוחות של העסק.
3. **Transaction:** תנועות פיננסיות (הכנסות, הוצאות) לניהול ה-Ledger.
4. **ServiceEvent:** אירועים או שירותים שניתנו ללקוחות (פגישות, פרויקטים וכדומה).
5. **RecurringBilling:** ניהול חיובים או תנועות שחוזרות על עצמן בתדירות מסוימת.
6. **UserSettings:** הגדרות אישיות של המשתמש במערכת.
7. **PushSubscription:** ניהול מנויים להתראות דחיפה (Push notifications).

**ראוטים - Controllers & Routes (`src/routes/`):**
- `authRoutes.ts`: התחברות וניהול טוקנים.
- `clientRoutes.ts`: פעולות CRUD ללקוחות.
- `transactionRoutes.ts`: יצירה, שליפה ועדכון של תנועות כספיות.
- `eventRoutes.ts`: ניהול אירועי השירות וחיבורם ללקוחות.
- `billingRoutes.ts`: ניהול החיובים החוזרים.
- `settingsRoutes.ts`: קריאה ועדכון של הגדרות.
- `pushRoutes.ts`: הרשמה וניהול התראות דחיפה.

## 4. מבנה צד הלקוח (Frontend)
הלקוח ממוקם בתיקיית `frontend/`.

**דפים (Pages) ב-`src/pages/`:**
- `Login.tsx`: מסך ההתחברות למערכת.
- `Dashboard.tsx`: מסך הבית המציג סיכום נתונים.
- `Clients.tsx`: רשימת לקוחות וניהולם.
- `Events.tsx`: ניהול אירועים/שירותים.
- `Ledger.tsx`: ספר החשבונות - הצגת כלל התנועות וההוצאות/הכנסות.
- `Balances.tsx`: ניהול יתרות/מאזנים פיננסיים.
- `RecurringBillings.tsx`: ניהול וצפייה בחיובים או תנועות מחזוריות.
- `CalendarPage.tsx`: תצוגת יומן של אירועים.
- `SettingsPage.tsx`: הגדרות מערכת ומשתמש.

## 5. איך המערכת עובדת? (Workflow & Architecture)
1. **זרימת מידע:** צד הלקוח מבוסס React-Redux ושואב את הנתונים דרך קריאות API משרת ה-Express. השרת פונה ל-MongoDB בעזרת Mongoose, מבצע וולידציה עם Zod ומחזיר JSON ללקוח.
2. **אוטומציה ומשימות רקע:** מערכת השרת (בעזרת `node-cron`) סורקת חיובים חוזרים (מטבלת `RecurringBilling`) ויוצרת במידת הצורך תנועות חדשות (`Transaction`), וכן מפעילה מנגנון התראות פוש לדפדפן.
3. **עיצוב וממשק:** הממשק נשען על חוקי חווית משתמש מדויקים מאוד המוגדרים ב-`design.md` - חובה לשמור על צבעי רקע נכונים (כגון Canvas Cream - `#F3F0EE`), ועל אלמנטים עגולים לחלוטין (Pills) על מנת ליישם חוויה ברמה גבוהה ומוקפדת.
4. **תמיכת PWA:** המערכת מוגדרת כך שניתן להתקינה ישירות על סמארטפונים ושולחנות עבודה.

## 6. הרצה מקומית (Development)
בתיקייה הראשית מוגדר קובץ `package.json` מרכזי המנהל את שני החלקים בעזרת `concurrently`:
- הפקודה `npm run dev` בתיקייה הראשית (`vault/`) תפעיל גם את השרת (פורט ברירת מחדל של ה-backend בדרך כלל) וגם את צד הלקוח (Vite) במקביל, ותדפיס את הלוגים של שניהם בצבעים שונים.
